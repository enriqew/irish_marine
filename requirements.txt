# Runtime dependencies — pinned for reproducible pipeline runs.
# Regenerate a full lock with: pip freeze > requirements-lock.txt
httpx==0.28.1
pandas==3.0.2
geopandas==1.1.3
shapely==2.1.2
# NOAA OISST monthly SST — xarray reads the NetCDF/OPeNDAP grid, numpy subsets it
xarray==2026.7.0
netCDF4==1.7.4
cftime==1.6.5
numpy==2.4.4
