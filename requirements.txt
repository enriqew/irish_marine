# Runtime dependencies — pinned for reproducible pipeline runs.
# Regenerate a full lock with: pip freeze > requirements-lock.txt
httpx==0.28.1
# Raw OBIS footprints: DuckDB queries the local GeoParquet dump; .df() needs pandas
duckdb==1.5.4
pandas==3.0.2
# NOAA OISST monthly SST — xarray reads the daily NetCDFs (from AWS Open Data via
# s3fs), numpy averages them to monthly means
xarray==2026.7.0
netCDF4==1.7.4
cftime==1.6.5
numpy==2.4.4
s3fs==2026.6.0
